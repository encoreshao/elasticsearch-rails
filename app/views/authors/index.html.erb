<p id="notice"><%= notice %></p>

<h1>
  Authors
  <small class='float-right'>
    <%= link_to 'New Author', new_author_path, class: 'btn btn-outline-info' %>
  </small>
</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th scope="col">First name</th>
      <th scope="col">Last name</th>
      <th scope="col"></th>
    </tr>
  </thead>

  <tbody>
    <% @authors.each do |author| %>
      <tr>
        <td><%= author.first_name %></td>
        <td><%= author.last_name %></td>
        <td>
          <div class='float-right'>
            <%= link_to 'Show', author, class: 'btn btn-outline-info' %>
            <%= link_to 'Edit', edit_author_path(author), class: 'btn btn-outline-info' %>
            <%= link_to 'Destroy', author, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-info' %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<nav aria-label="pagination">
  <%= paginate @authors %>
</nav>
